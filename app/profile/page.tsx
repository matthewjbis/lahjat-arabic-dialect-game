import { ProfileView } from "@/components/ProfileView";

export const metadata = {
  title: "Profile — Lahjat",
  description:
    "Your Lahjat profile — track your scores, streaks, and the dialect clips you've contributed.",
};

export default function ProfilePage() {
  return <ProfileView />;
}
